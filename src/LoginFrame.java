import javax.swing.*;

public class LoginFrame extends JFrame {
    public LoginFrame() {
        setTitle("Membership Login");
        setSize(300, 200);
        setLocationRelativeTo(null);
        setVisible(true);
    }
}
